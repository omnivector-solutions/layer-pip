includes:
- layer:basic
options:
  basic:
    packages:
    - python3-venv
defines:
  packages:
    type: array
    default: []
    description: Pip packages to be installed in the venv.
  env_name:
    description: |
      Name for the virtual env.

      Defaults to the juju application name.
    type: string
    default: ""
